React Email component example references copied from the upstream `resend/react-email`
gallery at `apps/web/components` on the `canary` branch.

These files are stored locally so the AI can read concrete component examples by
category and slug without relying on the live website.
